---
import Base from '../layouts/Base.astro';
---

<Base title="Rover — Castalia Institute" description="Autonomous Field Rover: a modular robotic platform for environmental sensing, terrain exploration, and autonomous navigation in high-desert terrain.">

<!-- ─── NAV ──────────────────────────────────────────────── -->
<nav>
  <div class="nav-inner">
    <a href="/" class="nav-brand">
      <span class="brand-mark">AFR</span>
      <span class="brand-text">Autonomous Field Rover</span>
    </a>
    <div class="nav-links">
      <a href="#overview">Overview</a>
      <a href="#architecture">Architecture</a>
      <a href="#sensors">Sensors</a>
      <a href="#autonomy">Autonomy</a>
      <a href="#phases">Phases</a>
      <a href="/courses">Courses</a>
    </div>
  </div>
</nav>

<!-- ─── HERO ─────────────────────────────────────────────── -->
<section class="hero">
  <div class="hero-bg">
    <img
      src="https://images.unsplash.com/photo-1615244895949-e0aa324843bb?w=1920&q=80&auto=format&fit=crop"
      alt="Great Sand Dunes, San Luis Valley, Colorado"
      loading="eager"
    />
    <div class="hero-overlay"></div>
  </div>
  <div class="hero-content">
    <div class="hero-badge">Castalia Institute &middot; Design Document v0.1</div>
    <h1>Autonomous<br />Field Rover</h1>
    <p class="hero-sub">
      A modular robotic platform for environmental sensing, terrain exploration,
      and autonomous navigation&mdash;built for the high desert.
    </p>
    <div class="hero-tags">
      <span>Environmental Sensing</span>
      <span>Terrain Mapping</span>
      <span>Autonomous Navigation</span>
      <span>Scientific Fieldwork</span>
    </div>
    <div class="hero-cta">
      <a href="#overview" class="btn btn-primary">Explore the Design</a>
      <a href="https://github.com/InquiryInstitute/rover.castalia.institute" class="btn btn-ghost">GitHub</a>
    </div>
  </div>
  <div class="hero-scroll">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
  </div>
</section>

<!-- ─── TERRAIN ──────────────────────────────────────────── -->
<section class="terrain-strip">
  <div class="terrain-inner">
    <div class="terrain-item">
      <span class="terrain-label">Desert Steppe</span>
    </div>
    <div class="terrain-divider"></div>
    <div class="terrain-item">
      <span class="terrain-label">Volcanic Gravel</span>
    </div>
    <div class="terrain-divider"></div>
    <div class="terrain-item">
      <span class="terrain-label">Alluvial Fans</span>
    </div>
    <div class="terrain-divider"></div>
    <div class="terrain-item">
      <span class="terrain-label">Scrubland</span>
    </div>
    <div class="terrain-divider"></div>
    <div class="terrain-item">
      <span class="terrain-label">San Luis Valley</span>
    </div>
  </div>
</section>

<!-- ─── OVERVIEW ─────────────────────────────────────────── -->
<section id="overview" class="section">
  <div class="container">
    <div class="section-header">
      <span class="section-num">01</span>
      <h2>Project Overview</h2>
    </div>

    <div class="grid-2">
      <div class="card card-image">
        <img
          src="/galaxyrvr.jpg"
          alt="SunFounder GalaxyRVR — 6-wheel rocker-bogie rover with solar panel"
          loading="lazy"
        />
      </div>
      <div>
        <p class="lead">
          The Autonomous Field Rover (AFR) is designed to operate reliably in dust, wind,
          temperature swings, and remote locations&mdash;conditions typical of the
          San Luis Valley and similar high-desert environments.
        </p>
        <div class="objectives-grid">
          <div class="objective-card">
            <div class="objective-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20M2 12h20"/></svg>
            </div>
            <h3>Autonomous Navigation</h3>
            <p>GPS waypoint following, SLAM-based mapping, and obstacle avoidance across unstructured terrain.</p>
          </div>
          <div class="objective-card">
            <div class="objective-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>
            </div>
            <h3>Environmental Sensing</h3>
            <p>Temperature, humidity, barometric pressure, VOC, and soil moisture data collection.</p>
          </div>
          <div class="objective-card">
            <div class="objective-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
            </div>
            <h3>Terrain Mapping</h3>
            <p>Stereo vision and LiDAR-based 3D terrain reconstruction for geological survey.</p>
          </div>
          <div class="objective-card">
            <div class="objective-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
            </div>
            <h3>Long-Duration Operation</h3>
            <p>LiFePO4 batteries with optional solar charging for 4&ndash;10 hour autonomous missions.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── ENVIRONMENT ──────────────────────────────────────── -->
<section class="env-section">
  <div class="container">
    <div class="env-grid">
      <div class="env-card">
        <div class="env-value">-20&deg;C&ndash;40&deg;C</div>
        <div class="env-label">Temperature Range</div>
      </div>
      <div class="env-card">
        <div class="env-value">High</div>
        <div class="env-label">Dust Exposure</div>
      </div>
      <div class="env-card">
        <div class="env-value">Strong</div>
        <div class="env-label">Wind Conditions</div>
      </div>
      <div class="env-card">
        <div class="env-value">Uneven</div>
        <div class="env-label">Terrain Profile</div>
      </div>
    </div>
  </div>
</section>

<!-- ─── ARCHITECTURE ─────────────────────────────────────── -->
<section id="architecture" class="section">
  <div class="container">
    <div class="section-header">
      <span class="section-num">02</span>
      <h2>System Architecture</h2>
    </div>

    <div class="arch-tree">
      <div class="arch-root">Rover System</div>
      <div class="arch-branches">
        <div class="arch-branch">
          <h3>Mobility Platform</h3>
          <ul>
            <li>Aluminum extrusion frame</li>
            <li>Sealed electronics compartment</li>
            <li>Differential or 4WD rocker-bogie</li>
            <li>Pneumatic ATV wheels (25&ndash;35 cm)</li>
          </ul>
          <div class="arch-spec">
            <div class="spec-row"><span>Length</span><span>80&ndash;120 cm</span></div>
            <div class="spec-row"><span>Width</span><span>60&ndash;80 cm</span></div>
            <div class="spec-row"><span>Height</span><span>40&ndash;70 cm</span></div>
            <div class="spec-row"><span>Weight</span><span>25&ndash;40 kg</span></div>
          </div>
        </div>
        <div class="arch-branch">
          <h3>Compute &amp; Control</h3>
          <ul>
            <li>NVIDIA Jetson Orin Nano (primary)</li>
            <li>Orange Pi 5 / Raspberry Pi 5</li>
            <li>ESP32-S3 microcontroller layer</li>
            <li>ROS2 + Nav2 stack</li>
          </ul>
          <div class="arch-spec">
            <div class="spec-row"><span>OS</span><span>Ubuntu / Debian</span></div>
            <div class="spec-row"><span>Framework</span><span>ROS2</span></div>
            <div class="spec-row"><span>Navigation</span><span>Nav2 + RTAB-Map</span></div>
            <div class="spec-row"><span>MCU</span><span>ESP32-S3 / STM32</span></div>
          </div>
        </div>
        <div class="arch-branch">
          <h3>Sensor Suite</h3>
          <ul>
            <li>u-blox ZED-F9P GNSS (RTK)</li>
            <li>Bosch BNO085 IMU</li>
            <li>OAK-D stereo camera</li>
            <li>BME688 environmental</li>
          </ul>
          <div class="arch-spec">
            <div class="spec-row"><span>GPS accuracy</span><span>~2 cm (RTK)</span></div>
            <div class="spec-row"><span>Vision</span><span>Stereo depth</span></div>
            <div class="spec-row"><span>Environmental</span><span>T / H / P / VOC</span></div>
            <div class="spec-row"><span>Soil</span><span>Moisture probe</span></div>
          </div>
        </div>
        <div class="arch-branch">
          <h3>Power System</h3>
          <ul>
            <li>LiFePO4 24V battery pack</li>
            <li>20&ndash;40 Ah capacity</li>
            <li>Optional 200&ndash;400W solar</li>
            <li>MPPT charge controller</li>
          </ul>
          <div class="arch-spec">
            <div class="spec-row"><span>Chemistry</span><span>LiFePO4</span></div>
            <div class="spec-row"><span>Voltage</span><span>24V</span></div>
            <div class="spec-row"><span>Runtime</span><span>4&ndash;10 hours</span></div>
            <div class="spec-row"><span>Solar</span><span>200&ndash;400W</span></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── REFERENCE PLATFORM ───────────────────────────────── -->
<section class="ref-platform">
  <div class="container">
    <div class="grid-2 grid-2-reverse">
      <div>
        <span class="ref-badge">Reference Platform</span>
        <h2>SunFounder GalaxyRVR</h2>
        <p class="lead">
          The GalaxyRVR Mars Rover Kit serves as a prototyping reference&mdash;a rocker-bogie
          suspension system, ESP32 camera, solar charging, and Arduino-based control
          in a compact, terrain-ready package.
        </p>
        <ul class="feature-list">
          <li>6-wheel rocker-bogie suspension (aluminum alloy)</li>
          <li>ESP32 CAM with real-time FPV</li>
          <li>Solar panel + rechargeable battery</li>
          <li>Ultrasonic &amp; infrared obstacle avoidance</li>
          <li>Arduino Uno R3 compatible</li>
          <li>RGB LED strips for low-light operation</li>
        </ul>
        <a href="https://www.sunfounder.com/products/sunfounder-galaxyrvr-mars-rover-kit" class="btn btn-primary" target="_blank" rel="noopener">SunFounder Product Page</a>
      </div>
      <div class="card card-image ref-image-card">
        <img
          src="/galaxyrvr.jpg"
          alt="SunFounder GalaxyRVR climbing volcanic rock with rocker-bogie suspension"
          loading="lazy"
        />
        <div class="ref-image-overlay">
          <span>Rocker-Bogie Suspension</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── SENSORS ───────────────────────────────────────────── -->
<section id="sensors" class="section">
  <div class="container">
    <div class="section-header">
      <span class="section-num">03</span>
      <h2>Sensor Suite &amp; Communications</h2>
    </div>

    <div class="sensor-grid">
      <div class="sensor-card sensor-card-wide">
        <div class="sensor-category">Navigation</div>
        <h3>GNSS &mdash; u-blox ZED-F9P</h3>
        <p>Multi-band RTK-capable receiver delivering ~2 cm positional accuracy for precise waypoint navigation and geo-referenced data collection.</p>
        <div class="sensor-specs">
          <span>RTK: ~2 cm</span>
          <span>Multi-band</span>
          <span>L1/L2</span>
        </div>
      </div>
      <div class="sensor-card">
        <div class="sensor-category">Navigation</div>
        <h3>IMU</h3>
        <p>Bosch BNO085 or ICM-20948 for orientation and dead reckoning between GPS fixes.</p>
        <div class="sensor-specs">
          <span>9-DOF</span>
          <span>Sensor fusion</span>
        </div>
      </div>
      <div class="sensor-card">
        <div class="sensor-category">Vision</div>
        <h3>Stereo Camera</h3>
        <p>OAK-D or ZED Mini for real-time depth mapping and obstacle detection.</p>
        <div class="sensor-specs">
          <span>Depth map</span>
          <span>Neural inference</span>
        </div>
      </div>
      <div class="sensor-card">
        <div class="sensor-category">Vision</div>
        <h3>Monocular Camera</h3>
        <p>Sony IMX sensor with M12 lens for terrain classification and navigation AI.</p>
        <div class="sensor-specs">
          <span>Classification</span>
          <span>Nav AI</span>
        </div>
      </div>
      <div class="sensor-card">
        <div class="sensor-category">Environmental</div>
        <h3>BME688</h3>
        <p>Temperature, humidity, barometric pressure, and VOC gas sensing in a single module.</p>
        <div class="sensor-specs">
          <span>T / H / P</span>
          <span>VOC</span>
        </div>
      </div>
      <div class="sensor-card sensor-card-wide">
        <div class="sensor-category">Communications</div>
        <h3>Multi-link Connectivity</h3>
        <p>Wi-Fi (100&ndash;300 m primary), LoRa or LTE for long-range, optional Starlink base station. Telemetry includes GPS, battery, speed, sensor data, and system health.</p>
        <div class="sensor-specs">
          <span>Wi-Fi</span>
          <span>LoRa</span>
          <span>LTE</span>
          <span>Starlink</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── AUTONOMY ──────────────────────────────────────────── -->
<section id="autonomy" class="section section-dark">
  <div class="container">
    <div class="section-header">
      <span class="section-num">04</span>
      <h2>Autonomy Modes</h2>
    </div>

    <div class="modes-grid">
      <div class="mode-card">
        <div class="mode-number">01</div>
        <h3>Manual</h3>
        <p>Direct teleoperation via Wi-Fi. Operator drives the rover remotely with live FPV camera feed and full sensor telemetry overlay.</p>
        <div class="mode-tag">Teleoperation</div>
      </div>
      <div class="mode-card mode-card-active">
        <div class="mode-number">02</div>
        <h3>Assisted</h3>
        <p>Human-directed with autonomous obstacle avoidance. The rover overrides commands that would cause collisions while maintaining operator intent.</p>
        <div class="mode-tag">Obstacle Avoidance</div>
      </div>
      <div class="mode-card">
        <div class="mode-number">03</div>
        <h3>Autonomous</h3>
        <p>Mission-based navigation: waypoint following, autonomous mapping, and survey task execution with full SLAM and path planning.</p>
        <div class="mode-tag">Mission Planning</div>
      </div>
    </div>

    <div class="software-stack">
      <h3>Software Stack</h3>
      <div class="stack-grid">
        <div class="stack-item">
          <code>ROS2</code>
          <span>Robotics framework</span>
        </div>
        <div class="stack-item">
          <code>Nav2</code>
          <span>Navigation stack</span>
        </div>
        <div class="stack-item">
          <code>RTAB-Map</code>
          <span>Visual SLAM</span>
        </div>
        <div class="stack-item">
          <code>ORB-SLAM</code>
          <span>Feature-based SLAM</span>
        </div>
        <div class="stack-item">
          <code>Ubuntu</code>
          <span>Operating system</span>
        </div>
        <div class="stack-item">
          <code>ESP-IDF</code>
          <span>MCU firmware</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── DATA ──────────────────────────────────────────────── -->
<section class="section">
  <div class="container">
    <div class="section-header">
      <span class="section-num">05</span>
      <h2>Data Collection</h2>
    </div>
    <div class="data-grid">
      <div class="data-card">
        <div class="data-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>
        </div>
        <h3>Imagery</h3>
        <p>Geo-tagged stereo and monocular photography for terrain classification and visual survey.</p>
      </div>
      <div class="data-card">
        <div class="data-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 3v18h18"/><path d="M7 16l4-8 4 4 6-10"/></svg>
        </div>
        <h3>Terrain Elevation</h3>
        <p>3D point clouds and elevation models from stereo reconstruction.</p>
      </div>
      <div class="data-card">
        <div class="data-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>
        </div>
        <h3>Environmental</h3>
        <p>Time-series measurements of temperature, humidity, pressure, VOC, and soil moisture.</p>
      </div>
      <div class="data-card">
        <div class="data-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 1 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
        </div>
        <h3>GPS Tracks</h3>
        <p>RTK-precision trajectory logging for mission replay and coverage analysis.</p>
      </div>
    </div>
  </div>
</section>

<!-- ─── PHASES ────────────────────────────────────────────── -->
<section id="phases" class="section section-dark">
  <div class="container">
    <div class="section-header">
      <span class="section-num">06</span>
      <h2>Development Phases</h2>
    </div>

    <div class="timeline">
      <div class="timeline-item">
        <div class="timeline-marker"></div>
        <div class="timeline-content">
          <div class="timeline-phase">Phase 1</div>
          <h3>Prototype</h3>
          <ul>
            <li>Basic driving and motor control</li>
            <li>Camera streaming (FPV)</li>
            <li>Teleoperation via Wi-Fi</li>
          </ul>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-marker"></div>
        <div class="timeline-content">
          <div class="timeline-phase">Phase 2</div>
          <h3>Sensor Integration</h3>
          <ul>
            <li>RTK GPS integration</li>
            <li>IMU fusion</li>
            <li>Environmental sensor array</li>
          </ul>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-marker"></div>
        <div class="timeline-content">
          <div class="timeline-phase">Phase 3</div>
          <h3>Autonomy</h3>
          <ul>
            <li>SLAM implementation</li>
            <li>Obstacle avoidance</li>
            <li>Waypoint navigation</li>
          </ul>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-marker"></div>
        <div class="timeline-content">
          <div class="timeline-phase">Phase 4</div>
          <h3>Field Deployment</h3>
          <ul>
            <li>Long-duration outdoor missions</li>
            <li>San Luis Valley field trials</li>
            <li>Data pipeline validation</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── BUDGET ────────────────────────────────────────────── -->
<section class="section">
  <div class="container">
    <div class="section-header">
      <span class="section-num">07</span>
      <h2>Estimated Budget</h2>
    </div>

    <div class="budget-table-wrap">
      <table class="budget-table">
        <thead>
          <tr>
            <th>Component</th>
            <th>Estimated Cost</th>
          </tr>
        </thead>
        <tbody>
          <tr><td>Chassis</td><td>$300</td></tr>
          <tr><td>Motors</td><td>$300</td></tr>
          <tr><td>Wheels</td><td>$150</td></tr>
          <tr><td>Jetson / SBC</td><td>$300</td></tr>
          <tr><td>Sensors</td><td>$400</td></tr>
          <tr><td>Battery</td><td>$250</td></tr>
          <tr><td>Misc Electronics</td><td>$200</td></tr>
        </tbody>
        <tfoot>
          <tr>
            <td>Estimated Total</td>
            <td>$1,800 &ndash; $2,500</td>
          </tr>
        </tfoot>
      </table>
    </div>
  </div>
</section>

<!-- ─── EXTENSIONS ────────────────────────────────────────── -->
<section class="section section-dark">
  <div class="container">
    <div class="section-header">
      <span class="section-num">08</span>
      <h2>Future Extensions</h2>
    </div>
    <div class="extensions-grid">
      <div class="ext-card">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10z"/><path d="M12 6v6l4 2"/></svg>
        <h3>Robotic Arm</h3>
        <p>Manipulator for sample collection and instrument placement.</p>
      </div>
      <div class="ext-card">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
        <h3>Soil Sampler</h3>
        <p>Automated core sampling for geological and biological analysis.</p>
      </div>
      <div class="ext-card">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/></svg>
        <h3>Drone Docking</h3>
        <p>Aerial survey companion with autonomous launch and recovery.</p>
      </div>
      <div class="ext-card">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 12h4l3-9 4 18 3-9h4"/></svg>
        <h3>Environmental Station</h3>
        <p>Deployable weather station for long-term monitoring.</p>
      </div>
      <div class="ext-card">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="3"/><circle cx="4" cy="8" r="2"/><circle cx="20" cy="8" r="2"/><circle cx="4" cy="16" r="2"/><circle cx="20" cy="16" r="2"/><path d="M6 8.5l3.5 2M14.5 10.5L18 8.5M6 15.5l3.5-2M14.5 13.5l3.5 2"/></svg>
        <h3>Swarm Robotics</h3>
        <p>Multi-rover coordination for large-area survey missions.</p>
      </div>
    </div>
  </div>
</section>

<!-- ─── FOOTER ────────────────────────────────────────────── -->
<footer>
  <div class="container">
    <div class="footer-inner">
      <div class="footer-brand">
        <span class="brand-mark">AFR</span>
        <span>Castalia Institute</span>
      </div>
      <div class="footer-links">
        <a href="https://castalia.institute">castalia.institute</a>
        <a href="https://github.com/InquiryInstitute/rover.castalia.institute">GitHub</a>
      </div>
    </div>
  </div>
</footer>

</Base>

<style>
  /* ─── NAV ─────────────────────────────────────── */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    background: rgba(5, 5, 8, 0.85);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--c-border);
  }
  .nav-inner {
    max-width: var(--max-w); margin: 0 auto;
    padding: 0.75rem 1.5rem;
    display: flex; align-items: center; justify-content: space-between;
  }
  .nav-brand { display: flex; align-items: center; gap: 0.75rem; color: var(--c-text); }
  .brand-mark {
    font-family: var(--font-mono); font-weight: 700; font-size: 0.8rem;
    background: var(--c-accent); color: var(--c-bg);
    padding: 0.25rem 0.5rem; border-radius: 4px; letter-spacing: 0.05em;
  }
  .brand-text { font-size: 0.85rem; font-weight: 500; color: var(--c-text-muted); }
  .nav-links { display: flex; gap: 1.5rem; }
  .nav-links a { font-size: 0.8rem; font-weight: 500; color: var(--c-text-muted); text-transform: uppercase; letter-spacing: 0.08em; }
  .nav-links a:hover { color: var(--c-accent); opacity: 1; }

  /* ─── HERO ────────────────────────────────────── */
  .hero {
    position: relative; min-height: 100vh;
    display: flex; align-items: center; justify-content: center;
    overflow: hidden;
  }
  .hero-bg { position: absolute; inset: 0; }
  .hero-bg img { width: 100%; height: 100%; object-fit: cover; }
  .hero-overlay {
    position: absolute; inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(5,5,8,0.5) 0%,
      rgba(5,5,8,0.7) 50%,
      rgba(5,5,8,0.95) 100%
    );
  }
  .hero-content {
    position: relative; z-index: 2;
    max-width: 800px; padding: 6rem 1.5rem 4rem; text-align: center;
  }
  .hero-badge {
    display: inline-block; font-size: 0.75rem; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.15em;
    color: var(--c-accent); margin-bottom: 1.5rem;
    border: 1px solid var(--c-accent-dim);
    padding: 0.4rem 1rem; border-radius: 100px;
    background: var(--c-accent-dim);
  }
  .hero h1 {
    font-size: clamp(2.5rem, 7vw, 5rem); font-weight: 700;
    line-height: 1.05; letter-spacing: -0.03em;
    margin-bottom: 1.25rem;
  }
  .hero-sub {
    font-size: 1.15rem; color: var(--c-text-muted);
    max-width: 560px; margin: 0 auto 2rem; line-height: 1.7;
  }
  .hero-tags {
    display: flex; flex-wrap: wrap; justify-content: center; gap: 0.5rem;
    margin-bottom: 2.5rem;
  }
  .hero-tags span {
    font-size: 0.75rem; font-weight: 500; text-transform: uppercase;
    letter-spacing: 0.08em; padding: 0.35rem 0.85rem;
    border: 1px solid var(--c-border); border-radius: 100px;
    color: var(--c-text-muted);
  }
  .hero-cta { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
  .hero-scroll {
    position: absolute; bottom: 2rem; left: 50%; transform: translateX(-50%);
    color: var(--c-text-muted); animation: bob 2s ease-in-out infinite;
  }
  @keyframes bob {
    0%, 100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(8px); }
  }

  /* ─── BUTTONS ─────────────────────────────────── */
  .btn {
    display: inline-flex; align-items: center;
    padding: 0.7rem 1.5rem; border-radius: 8px;
    font-size: 0.85rem; font-weight: 600; letter-spacing: 0.02em;
    transition: all 0.2s;
  }
  .btn-primary {
    background: var(--c-accent); color: var(--c-bg);
  }
  .btn-primary:hover { background: #e09820; opacity: 1; }
  .btn-ghost {
    border: 1px solid var(--c-border); color: var(--c-text);
  }
  .btn-ghost:hover { border-color: var(--c-text-muted); opacity: 1; }

  /* ─── TERRAIN STRIP ───────────────────────────── */
  .terrain-strip {
    border-top: 1px solid var(--c-border);
    border-bottom: 1px solid var(--c-border);
    background: var(--c-surface);
    padding: 1rem 0; overflow-x: auto;
  }
  .terrain-inner {
    display: flex; align-items: center; justify-content: center; gap: 1.5rem;
    max-width: var(--max-w); margin: 0 auto; padding: 0 1.5rem;
    min-width: max-content;
  }
  .terrain-label {
    font-size: 0.75rem; font-weight: 600; text-transform: uppercase;
    letter-spacing: 0.12em; color: var(--c-text-muted); white-space: nowrap;
  }
  .terrain-divider {
    width: 4px; height: 4px; border-radius: 50%;
    background: var(--c-text-muted); opacity: 0.4; flex-shrink: 0;
  }

  /* ─── SECTIONS ────────────────────────────────── */
  .section { padding: 6rem 0; }
  .section-dark { background: var(--c-surface); }
  .container { max-width: var(--max-w); margin: 0 auto; padding: 0 1.5rem; }
  .section-header { margin-bottom: 3rem; }
  .section-num {
    display: block; font-family: var(--font-mono); font-size: 0.75rem;
    color: var(--c-accent); margin-bottom: 0.5rem; letter-spacing: 0.1em;
  }
  .section-header h2 { font-size: 2rem; font-weight: 700; letter-spacing: -0.02em; }
  .lead { font-size: 1.05rem; color: var(--c-text-muted); margin-bottom: 2rem; }

  /* ─── GRID ────────────────────────────────────── */
  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; align-items: start; }
  .grid-2-reverse { direction: rtl; }
  .grid-2-reverse > * { direction: ltr; }

  /* ─── CARDS ───────────────────────────────────── */
  .card { background: var(--c-surface); border: 1px solid var(--c-border); border-radius: 12px; overflow: hidden; }
  .card-image img { width: 100%; height: 100%; object-fit: cover; aspect-ratio: 4/3; }

  /* ─── OBJECTIVES ──────────────────────────────── */
  .objectives-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }
  .objective-card {
    padding: 1.25rem; border: 1px solid var(--c-border); border-radius: 10px;
    background: var(--c-surface);
  }
  .objective-icon { color: var(--c-accent); margin-bottom: 0.75rem; }
  .objective-card h3 { font-size: 0.9rem; font-weight: 600; margin-bottom: 0.4rem; }
  .objective-card p { font-size: 0.8rem; color: var(--c-text-muted); line-height: 1.5; }

  /* ─── ENV ──────────────────────────────────────── */
  .env-section {
    padding: 3rem 0;
    background: linear-gradient(135deg, var(--c-accent-dim), transparent 60%);
    border-top: 1px solid var(--c-border);
    border-bottom: 1px solid var(--c-border);
  }
  .env-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; }
  .env-card { text-align: center; }
  .env-value { font-size: 1.5rem; font-weight: 700; color: var(--c-accent); margin-bottom: 0.25rem; }
  .env-label { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--c-text-muted); }

  /* ─── ARCHITECTURE TREE ───────────────────────── */
  .arch-tree { margin-top: 1rem; }
  .arch-root {
    font-family: var(--font-mono); font-size: 0.9rem; font-weight: 600;
    color: var(--c-accent); padding: 0.75rem 1.25rem;
    border: 1px solid var(--c-accent-dim); background: var(--c-accent-dim);
    border-radius: 8px; display: inline-block; margin-bottom: 2rem;
  }
  .arch-branches { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem; }
  .arch-branch {
    padding: 1.5rem; border: 1px solid var(--c-border); border-radius: 10px;
    background: var(--c-surface);
  }
  .arch-branch h3 { font-size: 0.95rem; font-weight: 600; margin-bottom: 1rem; color: var(--c-accent); }
  .arch-branch ul { list-style: none; margin-bottom: 1.25rem; }
  .arch-branch li {
    font-size: 0.8rem; color: var(--c-text-muted); padding: 0.3rem 0;
    padding-left: 1rem; position: relative;
  }
  .arch-branch li::before {
    content: ''; position: absolute; left: 0; top: 0.65rem;
    width: 5px; height: 5px; border-radius: 50%; background: var(--c-border);
  }
  .arch-spec { border-top: 1px solid var(--c-border); padding-top: 1rem; }
  .spec-row {
    display: flex; justify-content: space-between; font-size: 0.75rem;
    padding: 0.2rem 0;
  }
  .spec-row span:first-child { color: var(--c-text-muted); }
  .spec-row span:last-child { font-family: var(--font-mono); color: var(--c-text); font-weight: 500; }

  /* ─── REFERENCE PLATFORM ──────────────────────── */
  .ref-platform {
    padding: 6rem 0; border-top: 1px solid var(--c-border);
    background: linear-gradient(180deg, var(--c-bg) 0%, var(--c-surface) 100%);
  }
  .ref-badge {
    display: inline-block; font-size: 0.7rem; font-weight: 700;
    text-transform: uppercase; letter-spacing: 0.15em;
    color: var(--c-accent); margin-bottom: 0.75rem;
  }
  .ref-platform h2 { font-size: 1.8rem; font-weight: 700; margin-bottom: 1rem; letter-spacing: -0.02em; }
  .feature-list { list-style: none; margin-bottom: 2rem; }
  .feature-list li {
    padding: 0.5rem 0 0.5rem 1.5rem; font-size: 0.9rem;
    color: var(--c-text-muted); position: relative;
  }
  .feature-list li::before {
    content: ''; position: absolute; left: 0; top: 0.9rem;
    width: 8px; height: 2px; background: var(--c-accent);
  }
  .ref-image-card { position: relative; }
  .ref-image-overlay {
    position: absolute; bottom: 0; left: 0; right: 0;
    padding: 1rem 1.25rem;
    background: linear-gradient(transparent, rgba(5,5,8,0.9));
  }
  .ref-image-overlay span {
    font-size: 0.75rem; font-weight: 600; text-transform: uppercase;
    letter-spacing: 0.1em; color: var(--c-accent);
  }

  /* ─── SENSOR GRID ─────────────────────────────── */
  .sensor-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }
  .sensor-card {
    padding: 1.5rem; border: 1px solid var(--c-border); border-radius: 10px;
    background: var(--c-surface);
  }
  .sensor-card-wide { grid-column: span 2; }
  .sensor-category {
    font-size: 0.65rem; font-weight: 700; text-transform: uppercase;
    letter-spacing: 0.15em; color: var(--c-accent); margin-bottom: 0.5rem;
  }
  .sensor-card h3 { font-size: 1rem; font-weight: 600; margin-bottom: 0.5rem; }
  .sensor-card p { font-size: 0.85rem; color: var(--c-text-muted); margin-bottom: 1rem; }
  .sensor-specs { display: flex; gap: 0.5rem; flex-wrap: wrap; }
  .sensor-specs span {
    font-family: var(--font-mono); font-size: 0.7rem;
    padding: 0.25rem 0.6rem; border-radius: 4px;
    background: var(--c-accent-dim); color: var(--c-accent);
  }

  /* ─── MODES ───────────────────────────────────── */
  .modes-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-bottom: 4rem; }
  .mode-card {
    padding: 2rem; border: 1px solid var(--c-border); border-radius: 12px;
    background: var(--c-surface-2); transition: border-color 0.3s;
  }
  .mode-card:hover { border-color: var(--c-accent-dim); }
  .mode-card-active { border-color: var(--c-accent); }
  .mode-number {
    font-family: var(--font-mono); font-size: 2rem; font-weight: 700;
    color: var(--c-border); margin-bottom: 1rem;
  }
  .mode-card-active .mode-number { color: var(--c-accent); }
  .mode-card h3 { font-size: 1.15rem; font-weight: 600; margin-bottom: 0.75rem; }
  .mode-card p { font-size: 0.85rem; color: var(--c-text-muted); margin-bottom: 1.25rem; }
  .mode-tag {
    font-size: 0.7rem; font-weight: 600; text-transform: uppercase;
    letter-spacing: 0.1em; color: var(--c-accent);
    padding: 0.3rem 0.75rem; border-radius: 4px;
    background: var(--c-accent-dim); display: inline-block;
  }

  /* ─── SOFTWARE STACK ──────────────────────────── */
  .software-stack h3 { font-size: 1.1rem; font-weight: 600; margin-bottom: 1.5rem; }
  .stack-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 1rem; }
  .stack-item {
    text-align: center; padding: 1.25rem 0.75rem;
    border: 1px solid var(--c-border); border-radius: 8px;
    background: var(--c-surface-2);
  }
  .stack-item code {
    display: block; font-family: var(--font-mono); font-size: 0.85rem;
    font-weight: 500; color: var(--c-accent); margin-bottom: 0.4rem;
  }
  .stack-item span { font-size: 0.7rem; color: var(--c-text-muted); }

  /* ─── DATA ────────────────────────────────────── */
  .data-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; }
  .data-card {
    padding: 1.5rem; border: 1px solid var(--c-border); border-radius: 10px;
    background: var(--c-surface);
  }
  .data-icon { color: var(--c-accent); margin-bottom: 1rem; }
  .data-card h3 { font-size: 0.95rem; font-weight: 600; margin-bottom: 0.5rem; }
  .data-card p { font-size: 0.8rem; color: var(--c-text-muted); line-height: 1.5; }

  /* ─── TIMELINE ────────────────────────────────── */
  .timeline { position: relative; padding-left: 2rem; }
  .timeline::before {
    content: ''; position: absolute; left: 7px; top: 0; bottom: 0;
    width: 2px; background: var(--c-border);
  }
  .timeline-item { position: relative; margin-bottom: 2.5rem; }
  .timeline-item:last-child { margin-bottom: 0; }
  .timeline-marker {
    position: absolute; left: -2rem; top: 0.3rem;
    width: 16px; height: 16px; border-radius: 50%;
    border: 2px solid var(--c-accent); background: var(--c-surface);
  }
  .timeline-phase {
    font-family: var(--font-mono); font-size: 0.7rem; font-weight: 600;
    color: var(--c-accent); text-transform: uppercase; letter-spacing: 0.1em;
    margin-bottom: 0.25rem;
  }
  .timeline-content h3 { font-size: 1.1rem; font-weight: 600; margin-bottom: 0.75rem; }
  .timeline-content ul { list-style: none; }
  .timeline-content li {
    font-size: 0.85rem; color: var(--c-text-muted); padding: 0.25rem 0 0.25rem 1.25rem;
    position: relative;
  }
  .timeline-content li::before {
    content: ''; position: absolute; left: 0; top: 0.6rem;
    width: 6px; height: 2px; background: var(--c-border);
  }

  /* ─── BUDGET ──────────────────────────────────── */
  .budget-table-wrap { max-width: 560px; }
  .budget-table { width: 100%; border-collapse: collapse; }
  .budget-table th {
    text-align: left; font-size: 0.7rem; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.1em;
    color: var(--c-text-muted); padding: 0.75rem 0;
    border-bottom: 2px solid var(--c-border);
  }
  .budget-table td {
    padding: 0.65rem 0; font-size: 0.9rem;
    border-bottom: 1px solid var(--c-border);
  }
  .budget-table td:last-child { font-family: var(--font-mono); text-align: right; }
  .budget-table th:last-child { text-align: right; }
  .budget-table tfoot td {
    font-weight: 700; color: var(--c-accent);
    border-bottom: none; border-top: 2px solid var(--c-accent-dim);
    padding-top: 1rem;
  }

  /* ─── EXTENSIONS ──────────────────────────────── */
  .extensions-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 1.25rem; }
  .ext-card {
    padding: 1.5rem; border: 1px solid var(--c-border); border-radius: 10px;
    background: var(--c-surface-2); text-align: center;
    transition: border-color 0.3s;
  }
  .ext-card:hover { border-color: var(--c-accent-dim); }
  .ext-card svg { color: var(--c-accent); margin-bottom: 1rem; }
  .ext-card h3 { font-size: 0.9rem; font-weight: 600; margin-bottom: 0.5rem; }
  .ext-card p { font-size: 0.75rem; color: var(--c-text-muted); line-height: 1.5; }

  /* ─── FOOTER ──────────────────────────────────── */
  footer {
    padding: 2rem 0; border-top: 1px solid var(--c-border);
    background: var(--c-bg);
  }
  .footer-inner {
    display: flex; align-items: center; justify-content: space-between;
  }
  .footer-brand { display: flex; align-items: center; gap: 0.75rem; }
  .footer-brand span { font-size: 0.85rem; color: var(--c-text-muted); }
  .footer-links { display: flex; gap: 1.5rem; }
  .footer-links a { font-size: 0.8rem; color: var(--c-text-muted); }

  /* ─── RESPONSIVE ──────────────────────────────── */
  @media (max-width: 1024px) {
    .arch-branches { grid-template-columns: repeat(2, 1fr); }
    .sensor-grid { grid-template-columns: 1fr 1fr; }
    .sensor-card-wide { grid-column: span 2; }
    .modes-grid { grid-template-columns: 1fr; }
    .stack-grid { grid-template-columns: repeat(3, 1fr); }
    .data-grid { grid-template-columns: repeat(2, 1fr); }
    .extensions-grid { grid-template-columns: repeat(3, 1fr); }
    .env-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 768px) {
    .nav-links { display: none; }
    .grid-2 { grid-template-columns: 1fr; }
    .grid-2-reverse { direction: ltr; }
    .objectives-grid { grid-template-columns: 1fr; }
    .arch-branches { grid-template-columns: 1fr; }
    .sensor-grid { grid-template-columns: 1fr; }
    .sensor-card-wide { grid-column: span 1; }
    .stack-grid { grid-template-columns: repeat(2, 1fr); }
    .data-grid { grid-template-columns: 1fr; }
    .extensions-grid { grid-template-columns: 1fr 1fr; }
    .footer-inner { flex-direction: column; gap: 1rem; text-align: center; }
  }
</style>
